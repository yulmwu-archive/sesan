let str = 'Hello World';
// let str = "Hello World";

let num = 10;
let num2 = 3.141592;

let bool = true;
let bool2 = false;

let arr = [1, 2, 3, 4, 5];

let object = { name: 'John', age: 30 };

let function = func (x, y) {
    return x + y;
};

// let function = func (x, y) x + y;

println(
    join([str, num, num2, bool, bool2, arr, object, function(1, 2)], '\n')
);
