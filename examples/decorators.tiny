let skip = { skipCheckArguments: true };
let custom = { customDecorator: 'hello' };

@skip + custom
func foo() {
    println(decorator('foo'));

    let x = join(__builtin_arguments('foo'), ' ');

    capture();

    return func() {
        println(x);
    };
};

foo(1, 2, 3, 'Hello')();
