let str = 'Hello World';
let str2 = "Hello World";

let num = 10;
let num2 = 3.141592;

let bool = true;
let bool2 = false;

let arr = [1, 2, 3, 4, 5];

let hash = {
    name: 'John',
    age: 30
};

let function = func (x, y) {
    return x + y;
};

println(str, str2, num, num2, bool, bool2, arr, hash, function(1, 2));
