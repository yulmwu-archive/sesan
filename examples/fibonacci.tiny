let fib = func(n) {
    match(n, [
        [0, func(v) {
            return 0;
        }], 
        [1, func(v) {
            return 1;
        }]
    ], func(v) {
        return fib(n - 1) + fib(n - 2);
    });
};

println(fib(10));
