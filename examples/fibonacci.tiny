let fib = func(n) {
    if ((n == 0) || (n == 1)) {
        if (n == 0) {
            return 0;
        } else {
            return 1;
        }
    } else {
        return fib(n - 1) + fib(n - 2);
    }
};

println(fib(10));
