import('@std/lib');

let fib = func(n) {
    if ((n == 0) || (n == 1)) {
        if (n == 0) {
            0;
        } else {
            1;
        }
    } else {
        fib(n - 1) + fib(n - 2);
    }
};

println(fib(10));
