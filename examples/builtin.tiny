let x = 5 * 2;

if ((typeof x) == 'NUMBER') { x = x + 1; }

let option = options();

if (option <- 'allowEval') eval('println("안녕, 세상아!");');
if (option <- 'allowJavaScript') js('console.log("こんにちは、世界！");');

// For `convert` Function, See Examples -> Utility (`string`, `number`, `boolean`).

let object = {
    name: 'John',
    age: 30,
    girlFriend: void 0,
    hello: func () println('Hello, ' + object <- 'name')
};

object <- hello();
