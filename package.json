{
    "name": "@tsukiroku/tiny",
    "version": "0.2.1",
    "description": "Tiny interpreter",
    "main": "dist/index.js",
    "scripts": {
        "start": "npm run build && node ./dist/cli/index.js",
        "build": "if [ -d dist ]; then rm -rf dist; npm run build:tsc; else npm run build:tsc; fi",
        "build:windows": "@echo off && IF exist dist ( rd dist; npm run build:tsc ) ELSE ( npm run build:tsc ) && @echo on",
        "build:tsc": "tsc -p tsconfig.json",
        "test": "npm run build && node ./dist/tests/$TEST.test.js # export TEST=something",
        "npm:publish": "npm run build && npm publish"
    },
    "repository": {
        "type": "git",
        "url": "git+https://github.com/tsukiroku/tiny.git"
    },
    "keywords": [
        "typescript"
    ],
    "author": "tsukiroku",
    "license": "MIT",
    "bugs": {
        "url": "https://github.com/tsukiroku/tiny"
    },
    "homepage": "https://github.com/tsukiroku/tiny",
    "types": "dist/index.d.ts",
    "files": [
        "dist/**/*"
    ],
    "devDependencies": {
        "@types/node": "^18.0.0",
        "@types/prompt-sync": "^4.1.1"
    },
    "dependencies": {
        "colors": "^1.4.0",
        "prompt-sync": "^4.2.0"
    },
    "publishConfig": {
        "access": "public"
    }
}
